function vec = vecDiskRead(V, file, varargin)
% VECDISKREAD
%

% Stefan Wong 2012


	%Parse optional arguments, if any
	if(nargin > 2)

	end

	if(iscell(file))
		%There are multiple files to read,
	else
		%single file
	end


end 	%vecDiskRead()
