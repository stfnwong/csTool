function ipOpt = optParser(options)
% OPTPARSER (csImProc)
%
% Parse input options for csImProc objects

% Stefan Wong 2012

	 if(~iscell(options))
		error('Options must be in cell array');
	end

	for k = 1:length(options)
		if(ischar(


end 	%optParser()
